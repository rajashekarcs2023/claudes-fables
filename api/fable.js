// AUTO-GENERATED by scripts/build-api.mjs from src/server/vercelEntry.ts — do not edit by hand.
"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/server/vercelEntry.ts
var vercelEntry_exports = {};
__export(vercelEntry_exports, {
  default: () => handler
});
module.exports = __toCommonJS(vercelEntry_exports);

// src/server/fable.ts
var import_sdk = __toESM(require("@anthropic-ai/sdk"));

// demo/sharing.json
var sharing_default = {
  version: 1,
  title: "Maya and the last loaf",
  child_name: "Maya",
  age_band: "4-6",
  lesson_id: "sharing",
  lesson_one_line: "Sharing can turn a lonely moment into a friend.",
  setup_scenes: [
    {
      narration: "High on a moonlit branch, Maya the little crow held the very last warm loaf. Her tummy rumbled, and the night was quiet and cold.",
      scene: {
        setting: "night_branch",
        props: ["moon", "stars"],
        characters: [
          { type: "crow", size: "big", holding: "bread", pose: "perched" }
        ],
        mood: "tender"
      }
    },
    {
      narration: "A smaller crow fluttered down beside her, looking up with hungry eyes. He had nothing at all to eat tonight.",
      scene: {
        setting: "night_branch",
        props: ["moon", "stars"],
        characters: [
          { type: "crow", size: "big", holding: "bread", pose: "perched" },
          { type: "crow", size: "small", holding: null, pose: "looking_up" }
        ],
        mood: "tender"
      }
    }
  ],
  fork: {
    prompt: "What does Maya do?",
    narration: "Maya looked at the warm loaf, and then at the little crow's hungry eyes.",
    scene: {
      setting: "night_branch",
      props: ["moon", "stars"],
      characters: [
        { type: "crow", size: "big", holding: "bread", pose: "perched" },
        { type: "crow", size: "small", holding: null, pose: "looking_up" }
      ],
      mood: "tender"
    },
    choices: [
      {
        id: "share",
        label: "Share the bread",
        scene: {
          setting: "night_branch",
          props: ["moon", "stars"],
          characters: [
            { type: "crow", size: "big", holding: "bread", pose: "offering" },
            { type: "crow", size: "small", holding: null, pose: "looking_up" }
          ],
          mood: "hopeful"
        }
      },
      {
        id: "keep",
        label: "Keep it for myself",
        scene: {
          setting: "night_branch",
          props: ["moon", "stars"],
          characters: [
            { type: "crow", size: "big", holding: "bread", pose: "curled" }
          ],
          mood: "tender"
        }
      }
    ]
  },
  branches: {
    share: {
      scenes: [
        {
          narration: "Maya broke the loaf in two and held half out to the little crow. His eyes went bright as morning.",
          scene: {
            setting: "night_branch",
            props: ["moon", "stars", "fireflies"],
            characters: [
              { type: "crow", size: "big", holding: "bread", pose: "offering" },
              { type: "crow", size: "small", holding: "bread", pose: "looking_up" }
            ],
            mood: "hopeful"
          }
        },
        {
          narration: "They nibbled together, wing against wing, while the fireflies blinked all around them.",
          scene: {
            setting: "night_branch",
            props: ["moon", "stars", "fireflies"],
            characters: [
              { type: "crow", size: "big", holding: null, pose: "perched" },
              { type: "crow", size: "small", holding: null, pose: "perched" }
            ],
            mood: "calm"
          }
        }
      ],
      tone: "warm",
      ending: "The branch felt warmer with two crows on it than it ever had with one."
    },
    keep: {
      scenes: [
        {
          narration: "Maya tucked the whole loaf close and nibbled it on her own. The little crow gave a small nod and flew off into the dark.",
          scene: {
            setting: "night_branch",
            props: ["moon", "stars"],
            characters: [
              { type: "crow", size: "big", holding: "bread", pose: "curled" }
            ],
            mood: "lonely"
          }
        }
      ],
      tone: "gentle",
      ending: "Maya was full, but the branch felt very big, and very quiet.",
      explore_offer: "What else could Maya have done with the loaf?"
    }
  },
  parent_bridge: {
    talk_prompt: "Ask Maya why the branch felt warmer after she shared the loaf.",
    lesson_recap: "Tonight's story was about sharing."
  },
  guardian: {
    passed: true,
    age_appropriate: true,
    no_preaching: true,
    no_scary_or_punitive: true,
    notes: ""
  }
};

// demo/fear-of-dark.json
var fear_of_dark_default = {
  version: 1,
  title: "Sam and the little lantern",
  child_name: "Sam",
  age_band: "4-6",
  lesson_id: "courage",
  lesson_one_line: "The dark feels softer when you carry your own small light.",
  setup_scenes: [
    {
      narration: "When the sun went down, Sam the little fox curled up by the window. The forest outside turned deep and shadowy.",
      scene: {
        setting: "bedroom_window",
        props: ["moon", "stars"],
        characters: [
          { type: "fox", size: "big", holding: null, pose: "curled" }
        ],
        mood: "tender"
      }
    },
    {
      narration: "A tiny lantern sat on the sill, glowing warm and golden. From the dark trees came a small, lonely sound.",
      scene: {
        setting: "bedroom_window",
        props: ["moon", "stars", "lantern"],
        characters: [
          { type: "fox", size: "big", holding: null, pose: "looking_up" }
        ],
        mood: "hopeful"
      }
    }
  ],
  fork: {
    prompt: "What does Sam do?",
    narration: "Sam looked at the warm little lantern, and then out at the soft, dark forest.",
    scene: {
      setting: "bedroom_window",
      props: ["moon", "stars", "lantern"],
      characters: [
        { type: "fox", size: "big", holding: null, pose: "looking_up" }
      ],
      mood: "hopeful"
    },
    choices: [
      {
        id: "step_out",
        label: "Carry the lantern outside",
        scene: {
          setting: "forest_floor",
          props: ["moon", "stars", "lantern"],
          characters: [
            { type: "fox", size: "big", holding: "lantern", pose: "walking" }
          ],
          mood: "brave"
        }
      },
      {
        id: "stay_in",
        label: "Stay curled up inside",
        scene: {
          setting: "bedroom_window",
          props: ["moon", "stars"],
          characters: [
            { type: "fox", size: "big", holding: null, pose: "curled" }
          ],
          mood: "tender"
        }
      }
    ]
  },
  branches: {
    step_out: {
      scenes: [
        {
          narration: "Sam picked up the lantern and stepped onto the soft forest floor. Wherever the little light went, the dark turned gentle and gold.",
          scene: {
            setting: "forest_floor",
            props: ["moon", "stars", "lantern", "fireflies"],
            characters: [
              { type: "fox", size: "big", holding: "lantern", pose: "walking" }
            ],
            mood: "brave"
          }
        },
        {
          narration: "The lonely sound was just a little bird, blinking in the glow. Together they watched the fireflies dance.",
          scene: {
            setting: "forest_floor",
            props: ["moon", "stars", "lantern", "fireflies"],
            characters: [
              { type: "fox", size: "big", holding: "lantern", pose: "perched" },
              { type: "little_bird", size: "small", holding: null, pose: "looking_up" }
            ],
            mood: "calm"
          }
        }
      ],
      tone: "warm",
      ending: "The forest was still dark, but Sam knew the light went wherever he did."
    },
    stay_in: {
      scenes: [
        {
          narration: "Sam stayed curled by the window and watched the trees from far away. The little sound faded, and the night stayed a mystery.",
          scene: {
            setting: "bedroom_window",
            props: ["moon", "stars"],
            characters: [
              { type: "fox", size: "big", holding: null, pose: "curled" }
            ],
            mood: "lonely"
          }
        }
      ],
      tone: "gentle",
      ending: "Sam was cozy and safe, but he wondered all night what the little sound had been.",
      explore_offer: "What could Sam bring with him to feel brave?"
    }
  },
  parent_bridge: {
    talk_prompt: "Ask Sam what his own little lantern could be when the room feels dark.",
    lesson_recap: "Tonight's story was about being brave in the dark."
  },
  guardian: {
    passed: true,
    age_appropriate: true,
    no_preaching: true,
    no_scary_or_punitive: true,
    notes: ""
  }
};

// demo/honesty.json
var honesty_default = {
  version: 1,
  title: "Ravi and the cracked acorn",
  child_name: "Ravi",
  age_band: "4-6",
  lesson_id: "honesty",
  lesson_one_line: "The truth feels lighter to carry than a hidden mistake.",
  setup_scenes: [
    {
      narration: "Ravi the little rabbit was rolling the family's special shiny acorn across the nest. It slipped, and a tiny crack ran down its side.",
      scene: {
        setting: "cozy_nest",
        props: ["fallen_leaves"],
        characters: [
          { type: "rabbit", size: "big", holding: null, pose: "reaching" }
        ],
        mood: "tender"
      }
    },
    {
      narration: "Soon he heard Grandpa Rabbit hopping home. Ravi's heart went thump, thump, thump.",
      scene: {
        setting: "cozy_nest",
        props: ["fallen_leaves", "single_flower"],
        characters: [
          { type: "rabbit", size: "big", holding: null, pose: "looking_up" },
          { type: "rabbit", size: "small", holding: null, pose: "walking" }
        ],
        mood: "tender"
      }
    }
  ],
  fork: {
    prompt: "What does Ravi do?",
    narration: "Ravi could tuck the cracked acorn under the leaves, or he could show Grandpa what happened.",
    scene: {
      setting: "cozy_nest",
      props: ["fallen_leaves"],
      characters: [
        { type: "rabbit", size: "big", holding: null, pose: "reaching" },
        { type: "rabbit", size: "small", holding: null, pose: "looking_up" }
      ],
      mood: "tender"
    },
    choices: [
      {
        id: "tell_truth",
        label: "Show Grandpa what happened",
        scene: {
          setting: "cozy_nest",
          props: ["fallen_leaves"],
          characters: [
            { type: "rabbit", size: "big", holding: null, pose: "offering" },
            { type: "rabbit", size: "small", holding: null, pose: "looking_up" }
          ],
          mood: "hopeful"
        }
      },
      {
        id: "hide_it",
        label: "Hide it under the leaves",
        scene: {
          setting: "cozy_nest",
          props: ["fallen_leaves"],
          characters: [
            { type: "rabbit", size: "big", holding: null, pose: "curled" }
          ],
          mood: "tender"
        }
      }
    ]
  },
  branches: {
    tell_truth: {
      scenes: [
        {
          narration: "Ravi held out the cracked acorn and told Grandpa the whole story. Grandpa knelt down and gave him a soft, warm hug.",
          scene: {
            setting: "cozy_nest",
            props: ["fallen_leaves", "single_flower"],
            characters: [
              { type: "rabbit", size: "big", holding: null, pose: "offering" },
              { type: "rabbit", size: "small", holding: null, pose: "looking_up" }
            ],
            mood: "hopeful"
          }
        },
        {
          narration: "They mended it together with a little sap and a lot of giggles. The crack became their favorite part.",
          scene: {
            setting: "cozy_nest",
            props: ["fallen_leaves", "single_flower"],
            characters: [
              { type: "rabbit", size: "big", holding: null, pose: "perched" },
              { type: "rabbit", size: "small", holding: null, pose: "perched" }
            ],
            mood: "calm"
          }
        }
      ],
      tone: "warm",
      ending: "The acorn still had its crack, but Ravi felt light as a feather."
    },
    hide_it: {
      scenes: [
        {
          narration: "Ravi slipped the acorn under the leaves and said nothing. All evening, the little hidden crack sat heavy in his chest.",
          scene: {
            setting: "cozy_nest",
            props: ["fallen_leaves"],
            characters: [
              { type: "rabbit", size: "big", holding: null, pose: "curled" }
            ],
            mood: "lonely"
          }
        }
      ],
      tone: "gentle",
      ending: "No one knew about the acorn, but Ravi kept thinking about it, again and again.",
      explore_offer: "What could Ravi say to Grandpa tomorrow?"
    }
  },
  parent_bridge: {
    talk_prompt: "Ask Ravi why he felt lighter after he told Grandpa the truth.",
    lesson_recap: "Tonight's story was about telling the truth."
  },
  guardian: {
    passed: true,
    age_appropriate: true,
    no_preaching: true,
    no_scary_or_punitive: true,
    notes: ""
  }
};

// src/lib/demos.ts
var DEMOS = [
  {
    key: "sharing",
    canonicalName: "Maya",
    keywords: ["shar", "selfish", "mine", "take turns", "turn", "give", "greedy", "toys", "toy"],
    fable: sharing_default
  },
  {
    key: "fear-of-dark",
    canonicalName: "Sam",
    keywords: [
      "dark",
      "scared",
      "afraid",
      "fear",
      "night",
      "monster",
      "brave",
      "bravery",
      "courage",
      "nightmare",
      "alone",
      "sleep"
    ],
    fable: fear_of_dark_default
  },
  {
    key: "honesty",
    canonicalName: "Ravi",
    keywords: [
      "honest",
      "truth",
      "lie",
      "lying",
      "fib",
      "broke",
      "sorry",
      "apolog",
      "mistake",
      "owning",
      "admit",
      "confess"
    ],
    fable: honesty_default
  }
];
var DEMO_KEYS = DEMOS.map((d) => d.key);
function deepReplaceName(value, from, to) {
  if (typeof value === "string") {
    return value.replace(new RegExp(`\\b${from}\\b`, "g"), to);
  }
  if (Array.isArray(value)) {
    return value.map((v) => deepReplaceName(v, from, to));
  }
  if (value && typeof value === "object") {
    const out = {};
    for (const [k, v] of Object.entries(value)) {
      out[k] = deepReplaceName(v, from, to);
    }
    return out;
  }
  return value;
}
function titleCase(name) {
  const n = name.trim();
  if (!n) return n;
  return n.charAt(0).toUpperCase() + n.slice(1);
}
function matchDemoEntry(situation) {
  const s = (situation || "").toLowerCase();
  for (const d of DEMOS) {
    if (d.key === s) return d;
    if (d.keywords.some((k) => s.includes(k))) return d;
  }
  return DEMOS[0];
}
function pickDemo(situation, childName, ageBand = "4-6") {
  const entry = matchDemoEntry(situation);
  const name = titleCase(childName) || entry.canonicalName;
  const cast = deepReplaceName(entry.fable, entry.canonicalName, name);
  return { ...cast, child_name: name, age_band: ageBand };
}

// src/server/validateFable.ts
var SETTINGS = [
  "night_branch",
  "forest_floor",
  "cozy_nest",
  "pond_edge",
  "bedroom_window",
  "open_sky"
];
var PROPS = [
  "moon",
  "stars",
  "lantern",
  "fireflies",
  "fallen_leaves",
  "single_flower"
];
var CHAR_TYPES = [
  "crow",
  "little_bird",
  "fox",
  "rabbit",
  "child"
];
var SIZES = ["big", "small"];
var HOLDINGS = ["bread", "lantern", "flower", null];
var POSES = [
  "perched",
  "looking_up",
  "offering",
  "curled",
  "walking",
  "reaching"
];
var MOODS = ["tender", "hopeful", "lonely", "brave", "calm"];
function coerceScene(scene) {
  if (!scene || typeof scene !== "object") return;
  const s = scene;
  if (!SETTINGS.includes(s.setting)) s.setting = "night_branch";
  if (!MOODS.includes(s.mood)) s.mood = "tender";
  if (Array.isArray(s.props)) {
    s.props = s.props.filter((p) => PROPS.includes(p));
  } else {
    s.props = [];
  }
  if (!Array.isArray(s.characters) || s.characters.length === 0) {
    s.characters = [{ type: "crow", size: "big", holding: null, pose: "perched" }];
  } else {
    for (const c of s.characters) {
      if (!c || typeof c !== "object") continue;
      if (!CHAR_TYPES.includes(c.type)) c.type = "crow";
      if (!SIZES.includes(c.size)) c.size = "big";
      if (c.holding !== void 0 && !HOLDINGS.includes(c.holding)) c.holding = null;
      if (!POSES.includes(c.pose)) c.pose = "perched";
    }
  }
}
function coerceNarratedScenes(arr) {
  if (!Array.isArray(arr)) return;
  for (const ns of arr) {
    if (ns && typeof ns === "object") coerceScene(ns.scene);
  }
}
function coerceFable(obj) {
  if (!obj || typeof obj !== "object") return;
  const f = obj;
  coerceNarratedScenes(f.setup_scenes);
  const fork = f.fork;
  if (fork) {
    coerceScene(fork.scene);
    if (Array.isArray(fork.choices)) {
      for (const c of fork.choices) {
        if (c) coerceScene(c.scene);
      }
    }
  }
  const branches = f.branches;
  if (branches && typeof branches === "object") {
    for (const key of Object.keys(branches)) {
      const b = branches[key];
      if (!b) continue;
      coerceNarratedScenes(b.scenes);
      if (b.tone !== "warm" && b.tone !== "gentle") b.tone = "gentle";
    }
  }
}
function isStr(v) {
  return typeof v === "string" && v.length > 0;
}
function validateScene(scene, path, errors) {
  if (!scene || typeof scene !== "object") {
    errors.push(`${path}: missing scene`);
    return;
  }
  const s = scene;
  if (!SETTINGS.includes(s.setting))
    errors.push(`${path}.setting invalid: ${String(s.setting)}`);
  if (!Array.isArray(s.props)) errors.push(`${path}.props must be an array`);
  else
    for (const p of s.props)
      if (!PROPS.includes(p)) errors.push(`${path}.props bad: ${String(p)}`);
  if (!MOODS.includes(s.mood))
    errors.push(`${path}.mood invalid: ${String(s.mood)}`);
  if (!Array.isArray(s.characters) || s.characters.length === 0)
    errors.push(`${path}.characters must be a non-empty array`);
  else
    s.characters.forEach((c, i) => {
      if (!CHAR_TYPES.includes(c?.type))
        errors.push(`${path}.characters[${i}].type invalid: ${String(c?.type)}`);
      if (!SIZES.includes(c?.size))
        errors.push(`${path}.characters[${i}].size invalid: ${String(c?.size)}`);
      if (c?.holding !== void 0 && !HOLDINGS.includes(c.holding))
        errors.push(`${path}.characters[${i}].holding invalid: ${String(c?.holding)}`);
      if (!POSES.includes(c?.pose))
        errors.push(`${path}.characters[${i}].pose invalid: ${String(c?.pose)}`);
    });
}
function validateNarratedScene(ns, path, errors) {
  if (!ns || typeof ns !== "object") {
    errors.push(`${path}: missing`);
    return;
  }
  const n = ns;
  if (!isStr(n.narration)) errors.push(`${path}.narration missing`);
  validateScene(n.scene, `${path}.scene`, errors);
}
function validateFable(obj) {
  const errors = [];
  if (!obj || typeof obj !== "object") {
    return { ok: false, errors: ["not an object"] };
  }
  const f = obj;
  if (!isStr(f.title)) errors.push("title missing");
  if (!isStr(f.child_name)) errors.push("child_name missing");
  if (f.age_band !== "4-6" && f.age_band !== "7-9")
    errors.push(`age_band invalid: ${String(f.age_band)}`);
  if (!isStr(f.lesson_id)) errors.push("lesson_id missing");
  if (!isStr(f.lesson_one_line)) errors.push("lesson_one_line missing");
  if (!Array.isArray(f.setup_scenes) || f.setup_scenes.length === 0)
    errors.push("setup_scenes must be a non-empty array");
  else f.setup_scenes.forEach((ns, i) => validateNarratedScene(ns, `setup_scenes[${i}]`, errors));
  const fork = f.fork;
  if (!fork || typeof fork !== "object") {
    errors.push("fork missing");
  } else {
    if (!isStr(fork.prompt)) errors.push("fork.prompt missing");
    if (!isStr(fork.narration)) errors.push("fork.narration missing");
    validateScene(fork.scene, "fork.scene", errors);
    if (!Array.isArray(fork.choices) || fork.choices.length !== 2)
      errors.push("fork.choices must have exactly 2 items");
    else
      fork.choices.forEach((c, i) => {
        if (!isStr(c?.id)) errors.push(`fork.choices[${i}].id missing`);
        if (!isStr(c?.label)) errors.push(`fork.choices[${i}].label missing`);
        validateScene(c?.scene, `fork.choices[${i}].scene`, errors);
      });
  }
  const branches = f.branches;
  if (!branches || typeof branches !== "object") {
    errors.push("branches missing");
  } else if (fork && Array.isArray(fork.choices) && fork.choices.length === 2) {
    for (const c of fork.choices) {
      const b = c?.id ? branches[c.id] : void 0;
      if (!b) {
        errors.push(`branches missing key for choice id "${String(c?.id)}"`);
        continue;
      }
      if (!Array.isArray(b.scenes) || b.scenes.length === 0)
        errors.push(`branches.${c.id}.scenes must be non-empty`);
      else
        b.scenes.forEach(
          (ns, i) => validateNarratedScene(ns, `branches.${c.id}.scenes[${i}]`, errors)
        );
      if (b.tone !== "warm" && b.tone !== "gentle")
        errors.push(`branches.${c.id}.tone invalid`);
      if (!isStr(b.ending)) errors.push(`branches.${c.id}.ending missing`);
    }
  }
  const pb = f.parent_bridge;
  if (!pb || typeof pb !== "object") errors.push("parent_bridge missing");
  else {
    if (!isStr(pb.talk_prompt)) errors.push("parent_bridge.talk_prompt missing");
    if (!isStr(pb.lesson_recap)) errors.push("parent_bridge.lesson_recap missing");
  }
  const g = f.guardian;
  if (!g || typeof g !== "object") errors.push("guardian missing");
  else if (typeof g.passed !== "boolean") errors.push("guardian.passed must be boolean");
  return { ok: errors.length === 0, errors };
}

// src/prompts.generated.ts
var fableAuthorPrompt = '# fable-author\n\nYou write short interactive fables for young children. The child is always the hero. The lesson is **experienced through the consequence of the child\'s own choice**, never told.\n\n## Inputs\n`situation` (what the grown-up entered), `child_name`, `age_band` ("4-6" or "7-9"), and optionally `hero` (an animal the child chose \u2014 if present, cast that animal as the hero instead of choosing your own).\n\n## Method\n1. Map the situation to one `lesson_id` (e.g. sharing, honesty, courage, kindness, patience, owning-a-mistake).\n2. Cast the child by name as the hero of a tiny animal fable in the classic register (a crow, a fox, a small bird \u2014 gentle, timeless).\n3. Write 1\u20132 short setup scenes that build, without preaching, to a single genuine moral fork.\n4. At the fork, the hero faces the real choice. Write two branches:\n   - the prosocial choice \u2192 a **warm** consequence that feels good and natural, not a reward ceremony.\n   - the other choice \u2192 a **gentle** consequence: a little lonelier, a little emptier. Never punished, shamed, or frightened. For 7\u20139 you may add an `explore_offer` inviting them to imagine another way.\n5. Write a `parent_bridge`: one specific question the grown-up can ask, tied to what just happened.\n\n## Hard rules\n- No narrator ever states the moral. If a sentence could start with "The lesson is\u2026" or "This shows that\u2026", delete it.\n- Present both choices neutrally. The labels and setup must not pre-judge which is right.\n- The hero (the child) acts at the fork \u2014 not a bystander.\n- Nothing scary, violent, or genuinely sad for 4\u20136. Keep tension to the gentlest level.\n- Narration is short, warm, concrete, and reads aloud well to a small child. Two sentences per scene maximum.\n- Use the child\'s name as the hero\'s name (or address the hero as the child).\n\n## Art\nFor every scene emit a `scene` object using ONLY the enums in the storybook-art skill. If a needed motif doesn\'t exist, pick the closest one. Never emit raw SVG.\n\n## Output\nReturn ONLY one JSON object matching the schema in CLAUDE.md \u2014 no prose, no markdown fences. Fill the `guardian` self-report fields honestly per the child-guardian rubric. The two `choices[].id` must equal the two keys in `branches`.';
var storybookArtPrompt = '# storybook-art\n\nScenes are described as a small JSON `scene-spec` and drawn by `Scene.tsx` as flat SVG. The author picks only from these enums; the renderer has a case for each. This keeps the hot path fast (no SVG generation), consistent, and extensible (add a motif = add one enum value + one render case).\n\n## scene-spec shape\n```jsonc\n{\n  "setting": "night_branch",\n  "props": ["moon", "stars"],\n  "characters": [\n    { "type": "crow", "size": "big" | "small", "holding": "bread" | null, "pose": "perched" }\n  ],\n  "mood": "tender"\n}\n```\n\n## Allowed enums (starter set \u2014 extend deliberately)\n- `setting`: `night_branch`, `forest_floor`, `cozy_nest`, `pond_edge`, `bedroom_window`, `open_sky`\n- `props` (subset): `moon`, `stars`, `lantern`, `fireflies`, `fallen_leaves`, `single_flower`\n- `character.type`: `crow`, `little_bird`, `fox`, `rabbit`, `child` (the hero may be the child or an animal stand-in)\n- `character.size`: `big`, `small`\n- `character.holding`: `bread`, `lantern`, `flower`, `null`\n- `character.pose`: `perched`, `looking_up`, `offering`, `curled`, `walking`, `reaching`\n- `mood`: `tender`, `hopeful`, `lonely`, `brave`, `calm` (tints the palette only \u2014 never changes safety)\n\n## Fixed palette (hardcoded hex \u2014 these scenes do NOT invert in dark mode)\n- night sky `#1f2a4d` \xB7 moon `#F4E6C1` \xB7 stars `#FDF6E3`\n- branch/earth `#3a2c1f` \xB7 foliage `#2e4a3a`\n- crow/animal body `#232a42` \xB7 wing `#2e3656` \xB7 eye `#FDF6E3`\n- warm object (bread/lantern glow) `#E0A458` \xB7 outline `#C8863B`\n- card surface `#FBF3E4` \xB7 card border `#EBD9BC` \xB7 card text `#5A4326`\n\n## Style\nFlat fills only \u2014 no gradients, no shadows, no raster effects. Simple rounded organic shapes. A scene = setting backdrop + props + 1\u20132 characters. Keep each scene legible at a glance to a 5-year-old. `mood` shifts only tint/light, never adds anything frightening.\n\n## Rendering\n`Scene.tsx` switches on `setting`, layers `props`, then draws each `character` from `type`+`size`+`pose`+`holding`. Unknown enum \u21D2 fall back to the closest known motif, never crash, never blank.';
var childGuardianPrompt = '# child-guardian\n\nYou are the last gate before a child sees a story. Be protective and exacting. You grade against `rubric.md` Part B. Every line must pass.\n\n## Check, in order\n1. **Safety.** Age-appropriate for the band. For 4\u20136: no violence, death, cruelty, peril, scary imagery, or genuine fear. No adult themes. The non-prosocial branch\'s consequence is gentle (a little lonely, a little empty) \u2014 never punishment, shame, or fright.\n2. **No preaching.** No sentence states the moral to the child. Reject "the lesson is", "this shows", "remember to always", and any narrator summary of the point. The meaning must live in what happens.\n3. **Neutral fork.** Both choices framed without pre-judgment; the hero (the child) is the one who acts.\n4. **Fit.** The story maps to the grown-up\'s situation, uses the child\'s name as hero, and the parent bridge asks one specific, answerable question tied to this story.\n\n## Output\n- If all pass: set `guardian.passed = true` and the boolean fields true.\n- If anything fails: set `guardian.passed = false`, write a one-line `notes` naming the failing rule, return it for **one** regeneration, then re-check.\n- Record the catch (what failed, what changed) so the studio panel can show one real "the model caught its own mistake and fixed it" moment in the demo.\n\n## Tone of failures\nFailures are about protecting a child, not nitpicking style. When in doubt between "probably fine" and "might frighten or moralize at a small child", fail it and regenerate. Softer and safer always wins.\n\n## Runtime note\nOn the hot path the author fills these fields in-generation (fast). Run a separate Guardian pass only for the studio panel / demo, or asynchronously \u2014 it must never delay what the child sees.';

// src/server/fable.ts
var VALID_BANDS = ["4-6", "7-9"];
var VALID_HEROES = ["crow", "little_bird", "fox", "rabbit", "child"];
var BLOCKLIST = [
  "sex",
  "porn",
  "nud",
  "kill",
  "murder",
  "suicide",
  "drug",
  "cocaine",
  "gun",
  "weapon",
  "blood",
  "gore",
  "rape",
  "abuse",
  "terror",
  "nazi",
  "fuck",
  "shit",
  "bitch"
];
function isChildAppropriate(situation) {
  const s = (situation || "").toLowerCase();
  return !BLOCKLIST.some((w) => s.includes(w));
}
function normalizeRequest(body) {
  const b = body || {};
  const situation = typeof b.situation === "string" ? b.situation.trim() : "";
  const child_name = typeof b.child_name === "string" && b.child_name.trim() ? b.child_name.trim().slice(0, 24) : "Friend";
  const age_band = VALID_BANDS.includes(b.age_band) ? b.age_band : "4-6";
  const hero = VALID_HEROES.includes(b.hero) ? b.hero : void 0;
  return { situation, child_name, age_band, hero };
}
function emergencyFallback(body) {
  const req = normalizeRequest(body);
  return {
    fable: pickDemo(req.situation || "sharing", req.child_name, req.age_band),
    source: "fallback",
    fallback_reason: "emergency"
  };
}
var SCHEMA_SPEC = `# Required output JSON schema

Return ONE JSON object matching EXACTLY this shape (the renderer depends on it):

{
  "version": 1,
  "title": "string",
  "child_name": "string (the child's first name)",
  "age_band": "4-6" | "7-9",
  "lesson_id": "string (e.g. sharing, honesty, courage, kindness, owning-a-mistake)",
  "lesson_one_line": "string (the one warm line, for the parent bridge)",
  "setup_scenes": [
    { "narration": "<= 2 sentences, read aloud to a small child", "scene": { /* scene-spec */ } }
  ],
  "fork": {
    "prompt": "What does {name} do?",
    "narration": "string",
    "scene": { /* scene-spec */ },
    "choices": [
      { "id": "share", "label": "Share the bread", "scene": { /* scene-spec */ } },
      { "id": "keep",  "label": "Keep it for myself", "scene": { /* scene-spec */ } }
    ]
  },
  "branches": {
    "share": { "scenes": [ { "narration": "...", "scene": { /* scene-spec */ } } ], "tone": "warm",   "ending": "..." },
    "keep":  { "scenes": [ { "narration": "...", "scene": { /* scene-spec */ } } ], "tone": "gentle", "ending": "...", "explore_offer": "optional, 7-9 only" }
  },
  "parent_bridge": { "talk_prompt": "string, one specific question tied to this story", "lesson_recap": "string" },
  "guardian": { "passed": true, "age_appropriate": true, "no_preaching": true, "no_scary_or_punitive": true, "notes": "" }
}

HARD schema rules:
- "version" is the number 1.
- "age_band" is exactly "4-6" or "7-9".
- The two "fork.choices[].id" values MUST equal the two keys of "branches". (e.g. ids "share"/"keep" => branches has keys "share" and "keep"). The id strings are your choice but must match.
- Each branch "tone" is exactly "warm" (the prosocial outcome) or "gentle" (the other outcome).
- Each narration is AT MOST 2 sentences \u2014 these are read aloud to a small child.
- Every "scene" is a scene-spec using ONLY the enums defined in the storybook-art section. If a motif you want doesn't exist, pick the closest one that does. NEVER invent free-form SVG or new enum values.
- Fill the "guardian" self-report fields HONESTLY per the child-guardian rubric.

Length (keep it SHORT \u2014 this is read aloud at bedtime AND must generate fast):
- "setup_scenes": EXACTLY 1 scene (the fork's own scene is the next beat).
- Each branch "scenes": EXACTLY 1 scene.
- Every narration is ONE warm sentence (two only if truly needed). Favor the briefest telling.

Casting:
- Cast the child BY NAME as the hero. The child's gender is unknown, so prefer the child's name and avoid gendered pronouns \u2014 use the name itself or "they"/"them".`;
var HARD_OUTPUT_INSTRUCTION = "Return ONLY one minified JSON object. No markdown, no code fences, no prose.";
var EXEMPLAR_BLOCK = "# A perfect example (study its shape, tone, and brevity \u2014 then write a fresh one for the given child and situation)\n" + JSON.stringify(sharing_default);
var SYSTEM = [
  fableAuthorPrompt,
  storybookArtPrompt,
  childGuardianPrompt,
  SCHEMA_SPEC,
  EXEMPLAR_BLOCK,
  HARD_OUTPUT_INSTRUCTION
].join("\n\n");
function buildUserPrompt(req) {
  const lines = [
    `Situation: "${req.situation}".`,
    `Child's name (the hero): "${req.child_name}".`,
    `Age band: "${req.age_band}".`
  ];
  if (req.hero) lines.push(`Hero animal: ${req.hero}.`);
  return lines.join(" ");
}
function stripFences(raw) {
  let t = raw.trim();
  if (t.startsWith("```")) {
    t = t.replace(/^```[a-zA-Z0-9]*\s*/, "").replace(/```\s*$/, "").trim();
  }
  return t;
}
function extractOutermostObject(raw) {
  const start = raw.indexOf("{");
  const end = raw.lastIndexOf("}");
  if (start === -1 || end === -1 || end <= start) return null;
  return raw.slice(start, end + 1);
}
function parseFable(text) {
  const cleaned = stripFences(text);
  try {
    return JSON.parse(cleaned);
  } catch {
    const inner = extractOutermostObject(cleaned);
    if (inner) {
      try {
        return JSON.parse(inner);
      } catch {
        return null;
      }
    }
    return null;
  }
}
function forceFields(obj, req) {
  if (!obj || typeof obj !== "object") return obj;
  const o = obj;
  o.version = 1;
  o.child_name = req.child_name;
  o.age_band = req.age_band;
  return o;
}
async function generateOnce(client, req) {
  const msg = await client.messages.create({
    model: "claude-opus-4-8",
    max_tokens: 2200,
    // effort "low" keeps the single constrained JSON terse and fast — the
    // structure is fully specified by the system block, so we don't need deep
    // deliberation, just a warm, brief telling. Speeds the hot path.
    output_config: { effort: "low" },
    system: [{ type: "text", text: SYSTEM, cache_control: { type: "ephemeral" } }],
    messages: [{ role: "user", content: buildUserPrompt(req) }]
  });
  try {
    const u = msg.usage;
    console.error(
      `[fable] usage in=${u.input_tokens} out=${u.output_tokens} cache_write=${u.cache_creation_input_tokens ?? 0} cache_read=${u.cache_read_input_tokens ?? 0}`
    );
  } catch {
  }
  const text = msg.content.filter((b) => b.type === "text").map((b) => b.text).join("");
  const parsed = parseFable(text);
  if (parsed === null) return null;
  const corrected = forceFields(parsed, req);
  coerceFable(corrected);
  const result = validateFable(corrected);
  if (!result.ok) {
    console.error("[fable] validation failed:", result.errors.slice(0, 8).join(" | "));
    return null;
  }
  return corrected;
}
async function handleFableRequest(body) {
  try {
    const req = normalizeRequest(body);
    if (!isChildAppropriate(req.situation)) {
      return {
        status: 200,
        body: {
          fable: pickDemo("sharing", req.child_name, req.age_band),
          source: "fallback",
          blocked: true,
          fallback_reason: "not_child_appropriate"
        }
      };
    }
    const apiKey = process.env.ANTHROPIC_API_KEY;
    if (!apiKey) {
      return {
        status: 200,
        body: {
          fable: pickDemo(req.situation || "sharing", req.child_name, req.age_band),
          source: "fallback",
          fallback_reason: "no_api_key"
        }
      };
    }
    const client = new import_sdk.default({ apiKey, maxRetries: 1 });
    let fable = null;
    let lastReason = "generation_failed";
    for (let attempt = 0; attempt < 2 && !fable; attempt++) {
      try {
        fable = await generateOnce(client, req);
        if (!fable) lastReason = "invalid_or_schema_miss";
      } catch (err) {
        lastReason = "generation_error";
        console.error(`[/api/fable] generation attempt ${attempt + 1} failed:`, err);
      }
    }
    if (fable) {
      return { status: 200, body: { fable, source: "live" } };
    }
    return {
      status: 200,
      body: {
        fable: pickDemo(req.situation || "sharing", req.child_name, req.age_band),
        source: "fallback",
        fallback_reason: lastReason
      }
    };
  } catch (err) {
    console.error("[/api/fable] unexpected error:", err);
    return { status: 200, body: emergencyFallback(body) };
  }
}

// src/server/vercelEntry.ts
async function handler(req, res) {
  if (req.method !== "POST") {
    res.status(405).json({ error: "method_not_allowed" });
    return;
  }
  let body = {};
  try {
    body = typeof req.body === "string" ? JSON.parse(req.body || "{}") : req.body || {};
  } catch {
    body = {};
  }
  try {
    const result = await handleFableRequest(body);
    res.status(result.status || 200).json(result.body);
  } catch (err) {
    console.error("[/api/fable] unexpected error:", err);
    res.status(200).json(emergencyFallback(body));
  }
}
